import 'package:flutter/material.dart';

class AdsScreen1 extends StatelessWidget {
  const AdsScreen1({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text('Ads Screen 1'),
      ),
      body: Center(
        child: Text('Ads Screen 1'),
      )
    );
  }
}
